import React from "react";
import { ProjectForm } from "@/components/projects/project-form";

export const metadata = {
  title: "Buat Proyek Portofolio Baru | GrowthCoder CMS",
};

export default function NewProjectPage() {
  return <ProjectForm />;
}
